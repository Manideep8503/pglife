# pglife

This is the pglife static website project. It contains city folders and `pgphoto` assets.

How to push to remote:

1. Create a GitHub repository (empty).
2. Run:

```
git remote add origin <REMOTE_URL>
git push -u origin main
```
